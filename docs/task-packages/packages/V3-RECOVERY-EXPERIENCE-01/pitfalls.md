# V3-RECOVERY-EXPERIENCE-01 易错点

- `recovered` 必须由快照和事件游标证明。
- 心跳不是业务事件，不能推进 Mission。
- 恢复测试不能直接注入最终 UI 状态。
- 本地缓存只能显示最后已确认状态，不能把待发送输入或过期报价标记为服务端已接受。
- 快照版本与事件游标不一致时必须进入失败/重试状态，不能选择较新的视觉结果覆盖权威事件。
- Worker 重领任务必须保持幂等，不能产生第二个 terminal event 或重复审批副作用。
