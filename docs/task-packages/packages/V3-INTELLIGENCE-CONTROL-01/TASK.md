# V3-INTELLIGENCE-CONTROL-01 Agent 决策控制（中层）

## 目标
控制澄清、混合检索、证据候选、比较、审批和行动解释的用户可见契约。
## 状态
`complete`
## 范围
- 只公开可验证执行阶段；工具结果必须类型化并可追溯。
## 非目标
- 不展示思维链，不引入多 Agent，不让模型生成商业事实。
## 前置依赖
- `V3-PRODUCT-CONTROL-01`、`V3-AGENT-01`、`V3-DATA-RAG-01`。
## 路径所有权
- 本包及 Agent 进度、行动审批两个小层包。
## 现状证据
- 单 Agent、十工具和混合检索已实现，交互呈现仍需确认。
## 执行步骤
1. 确认澄清与公开进度。2. 确认审批、询价和外跳。3. 对齐真实模型门禁。
## 数据引用
- `../../shared/business-data.json`
- `../../shared/development-data.json`
- `../../shared/pitfalls.md`
## 验收
- [x] Agent 进度和行动审批子包边界已冻结。
- [x] DEMO、FEED、LIVE 证据等级已写入确认点。
## 回滚
- 只回滚交互方案，不更改冻结工具权限边界。
## 停止条件
- 方案要求展示内部思维链或绕过审批时停止。
## 交接格式
- Agent 状态、工具契约、用户确认、模型门禁、下一包。
