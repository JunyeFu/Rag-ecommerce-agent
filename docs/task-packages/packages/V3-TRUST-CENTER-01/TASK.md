# V3-TRUST-CENTER-01 我的与信任中心（小层 05）

## 目标
确认“我的”入口中的模型、数据、报价证据、运行连接、隐私同意和数据删除信任中心，让用户能够准确判断当前系统能力边界。
## 状态
`complete`
## 范围
- 将模型模式、商品数据集、报价证据等级与运行连接分开呈现，避免把“已连接”误读为真实模型或 LIVE 报价。
- 展示任务历史和匿名质量数据授权状态，并允许进入管理。
- 展示真实数据删除入口、影响范围、执行状态以及契约版本。
- 保留任务、决策、我的全局导航，并以“我的”为当前入口。
## 非目标
- 不增加头像社交、会员、积分、优惠券、订单、营销、生产 OAuth 或 SSO；不显示密钥、主机地址或本机路径。
## 前置依赖
- `V3-EXPERIENCE-CONTROL-01`、`V3-ANDROID-01`。
## 路径所有权
- Android 我的页、删除 API 客户端及本包预览证据。
## 现状证据
- 小层 04 已确认；我的页已有基础声明，当前需要冻结“能力证据”和“基础设施连接”分离的信任结构。
## 执行步骤
1. 生图。2. 用户确认。3. 对齐运行声明、同意状态与删除 API。4. 用户隔离和删除链测试。
## 数据引用
- `../../shared/business-data.json`
- `../../shared/development-data.json`
- `../../shared/pitfalls.md`
## 验收
- [x] 用户确认预览和页面职责。
- [x] 当前模型、数据集、报价证据等级与连接状态可独立辨认。
- [x] 同意状态可管理，删除操作调用真实删除链并返回可恢复查询的执行状态。
## 回滚
- 回滚页面布局，不撤销数据删除安全边界。
## 停止条件
- 方案隐藏数据来源、把连接状态宣传成 LIVE 能力或要求展示本机敏感配置时停止。
## 交接格式
- 预览、声明字段、删除证据、用户决策、外部门禁。
