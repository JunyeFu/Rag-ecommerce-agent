# V3-TRUST-CENTER-01 易错点

- “已连接”不等于真实模型或 LIVE 报价可用。
- 删除按钮必须调用真实删除链，不只清本地界面。
- 不显示密钥、主机绝对路径或签名信息。
- 模型、数据集和报价证据必须分别标注，不能用一个笼统“演示模式”掩盖差异。
- 数据删除需展示范围并进入不可逆操作确认，不能把单击按钮直接等同于删除成功。
- 同意开关必须来自服务端状态，不能只保存在本地偏好中。
