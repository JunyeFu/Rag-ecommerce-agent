# V2-SECURITY-01 易错点

- 不读取旧仓或本机忽略文件来“确认”是否有凭据；只验证忽略规则和 secret scanner。
- 提示注入防护不能只靠系统提示，必须限制工具、参数、来源和运行预算。
- URL allowlist 必须覆盖解析、重定向、Unicode/punycode、DNS 和私网地址。
- 应用层限流不能代替生产共享限流和受信代理配置。
- 本地安全测试通过不等于独立渗透、法律或商店审核通过。
