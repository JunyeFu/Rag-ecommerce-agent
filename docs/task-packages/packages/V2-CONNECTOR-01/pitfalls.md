# V2-CONNECTOR-01 易错点

- fixture PASS 不代表 live API 权限、联盟结算或真实价格正确。
- 官方产品信息接口不一定等于联盟商品搜索或推广取链接口，必须按能力声明。
- 缓存命中不应刷新 `observed_at`；否则会把旧报价伪装为新报价。
- HTTP 302 的最终 host 仍需校验，不能只验证初始 URL。
- 连接器异常日志不得包含 app secret、签名串、完整授权响应或用户标识。
