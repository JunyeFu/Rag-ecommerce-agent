# V3-MISSION-WORKBENCH-01 易错点

- 同一 viewport 只能有一个完整推荐框；下一框只可露出导航性边缘。
- 推荐框内部不得再使用会和纵向 Pager 冲突的长列表滚动。
- 预览中的价格必须标注 DEMO，不得暗示真实报价。
- 主、次、再次排序必须来自可复现比较结果，不由 UI 或模型自由改写。
- 进程恢复后必须回到原推荐索引，不用直接注入 UI 状态冒充通过。
