# V2-AGENT-01 易错点

- graph 节点变多或引入多个模型不等于 Agent 可靠性提升。
- checkpointer 只有在 thread/run ID、幂等和事件顺序一致时才真正支持恢复。
- 工具返回的外部文本始终是不可信数据，不能拼入系统指令。
- 不保存思维链；Trace 只保存决策摘要、工具、证据、版本和结果。
- “加入购物车”是可逆本地写入；“打开商家”必须由用户点击，Agent 不自动导航。
