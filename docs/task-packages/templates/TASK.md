# <PACKAGE-ID> <任务名称>

## 目标

说明可验证的单一结果。

## 状态

`planned`

## 范围

- 列出本包必须完成的行为和产物。

## 非目标

- 列出明确不属于本包的能力、外部系统和发布结论。

## 前置依赖

- 列出 manifest 中的依赖包及所需证据。

## 路径所有权

- 列出本包允许新增或修改的目录。

## 现状证据

- 引用当前源码、基线、测试或运行证据，不引用无当前验证的旧结论。

## 执行步骤

1. 定义契约和失败语义。
2. 先写确定性测试或夹具。
3. 实现最小闭环。
4. 运行包内和全局验证。
5. 写入证据并更新状态。

## 数据引用

- `../../shared/business-data.json`
- `../../shared/development-data.json`
- `../../shared/pitfalls.md`

## 验收

- [ ] 实现范围内的行为通过。
- [ ] 失败和降级行为通过。
- [ ] evidence 与 manifest 状态一致。
- [ ] 未输出凭据或敏感环境值。

## 回滚

- 描述只回滚本包路径和迁移的方式，不触碰其他任务包或旧仓。

## 停止条件

- 依赖包未完成、需要未授权凭据、数据许可不明或路径所有权冲突时停止。

## 交接格式

- 结果：
- 变更路径：
- 验证命令与结果：
- 剩余外部门禁：
- 风险与下一包：
